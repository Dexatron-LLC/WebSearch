[project]
name = "web-navigator-mcp"
version = "0.1.0"
description = "Simple MCP server for web navigation using Playwright"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp>=1.25.0",
    "playwright>=1.57.0",
]

[project.scripts]
web-navigator-mcp = "server:main"
